import 'package:flutter/material.dart';

const loading_image =Image(
  width: 50,
  image: AssetImage("assets/images/loader.gif"),
);


const error_icon = Icon(
  Icons.error,
  color: Colors.redAccent,
  weight: 50,
);